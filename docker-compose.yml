services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: aether
      POSTGRES_PASSWORD: aether
      POSTGRES_DB: aether_clinics
    ports:
      - "5432:5432"
    volumes:
      - aether_pg:/var/lib/postgresql/data

volumes:
  aether_pg:
